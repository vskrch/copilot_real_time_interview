# OpenAI API Key (used as fallback)
OPENAI_API_KEY=sk-...

# Gemini API Key (primary AI provider)
GEMINI_API_KEY=AIzaSyCOpcx3f-8-eZ4-QD6gWkVTi4hThcJKz4c

# Set to "true" to use local Whisper.cpp transcription instead of OpenAI API
USE_LOCAL_TRANSCRIPTION=true

# Whisper model configuration (only used if USE_LOCAL_TRANSCRIPTION=true)
WHISPER_MODEL_SIZE=base  # Options: tiny, base, small, medium, large-v2
WHISPER_DEVICE=auto      # Options: cpu, cuda, auto (will use GPU if available)
WHISPER_COMPUTE_TYPE=float16  # Options: float16, float32, int8

# AI Provider Configuration
# Set these to "true" to force using OpenAI for specific features
USE_OPENAI_FOR_TEXT=false
USE_OPENAI_FOR_IMAGES=false
USE_OPENAI_FOR_SUMMARY=false
